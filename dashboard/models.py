"""The dashboard app is presentation-only; it defines no models of its own.

It reads and writes the models owned by the other apps (catalog, orders,
accounts, marketing, cms, delivery, core, payments, notifications, reports).
"""
